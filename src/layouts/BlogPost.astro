---
import type { CollectionEntry } from "astro:content";
import BaseHead from "../components/base/BaseHead.astro";
import Footer from "../components/base/Footer.astro";
import Navigation from "../components/base/Navigation.astro";
import Button from "../components/ui/Button.astro";
import { getBlogHeroImage } from "../utils/blogImages";
import {
  getLangFromUrl,
  useTranslations,
  useTranslatedPath,
} from "../i18n/utils";
import type { ui } from "../i18n/translations";

type Props = (CollectionEntry<"blogDe"> | CollectionEntry<"blogEn">)["data"] & {
  lang?: keyof typeof ui;
};

const {
  title,
  description,
  author,
  pubDate,
  updatedDate,
  heroImageKey,
  faq,
  lang: langProp,
} = Astro.props;
const lang: keyof typeof ui = langProp ?? getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const heroImage = heroImageKey ? getBlogHeroImage(heroImageKey) : undefined;
const heroUrl = heroImage
  ? typeof heroImage.src === "object"
    ? heroImage.src.src
    : heroImage.src
  : "/blog-placeholder-3.jpg";

function formatDate(date: Date, locale: string): string {
  return new Intl.DateTimeFormat(locale, {
    day: "numeric",
    month: "long",
    year: "numeric",
  }).format(date);
}

/* FAQPage JSON-LD: emitted from frontmatter `faq` (keep in sync with the visible MDX FAQ). */
const faqSchema =
  faq && faq.length > 0
    ? {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        mainEntity: faq.map((item) => ({
          "@type": "Question",
          name: item.question,
          acceptedAnswer: {
            "@type": "Answer",
            text: item.answer,
          },
        })),
      }
    : undefined;
---

<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead title={title} description={description} image={heroUrl} lang={lang} />
    {
      faqSchema && (
        <script
          type="application/ld+json"
          set:html={JSON.stringify(faqSchema)}
        />
      )
    }
    <style is:global>
      /*
        MDX <Image /> renders as direct <img> siblings under .blog-content (not inside <p>).
        Mobile: one column. md+: text blocks full width; consecutive images form a 2-column row.
      */
      .blog-content {
        display: grid;
        grid-template-columns: 1fr;
      }

      .blog-content > img {
        display: block;
        width: 100%;
        max-width: 42rem;
        height: auto;
        margin-top: 2rem;
        margin-bottom: 2rem;
        margin-left: auto;
        margin-right: auto;
        border-radius: 0.75rem;
        box-shadow:
          0 10px 15px -3px rgb(0 0 0 / 0.1),
          0 4px 6px -4px rgb(0 0 0 / 0.1);
        object-fit: cover;
      }

      /* Responsive row: 1 col → 2×2 → 4 in one row (e.g. cocktail gallery in MDX). */
      .blog-content > .blog-image-row {
        grid-column: 1 / -1;
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
        margin-top: 2rem;
        margin-bottom: 2rem;
      }

      .blog-content > .blog-image-row img {
        display: block;
        width: 100%;
        max-width: none;
        height: auto;
        margin: 0;
        border-radius: 0.75rem;
        box-shadow:
          0 10px 15px -3px rgb(0 0 0 / 0.1),
          0 4px 6px -4px rgb(0 0 0 / 0.1);
        object-fit: cover;
      }

      .blog-content > .blog-table-wrap,
      .blog-content > table {
        grid-column: 1 / -1;
        width: 100%;
      }

      .blog-content > .blog-table-wrap {
        overflow-x: auto;
        margin-top: 1rem;
        margin-bottom: 1.5rem;
        -webkit-overflow-scrolling: touch;
      }

      .blog-content table {
        width: 100%;
        border-collapse: collapse;
        table-layout: fixed;
        font-size: inherit;
        line-height: 1.5;
      }

      .blog-content table th,
      .blog-content table td {
        padding: 0.75rem 1rem;
        border-bottom: 1px solid rgb(226 232 240);
        text-align: left;
        vertical-align: top;
      }

      .blog-content table th {
        font-weight: 600;
        color: rgb(15 23 42);
        background-color: rgb(241 245 249);
        border-bottom: 2px solid rgb(203 213 225);
      }

      .blog-content table tbody tr:last-child td {
        border-bottom: none;
      }

      .blog-content table th:nth-child(1),
      .blog-content table td:nth-child(1) {
        width: 9rem;
        white-space: nowrap;
      }

      @media (max-width: 639px) {
        .blog-content table {
          min-width: 20rem;
        }
      }

      @media (min-width: 768px) {
        .blog-content > .blog-image-row {
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 1.5rem;
        }
      }

      @media (min-width: 1024px) {
        .blog-content > .blog-image-row {
          grid-template-columns: repeat(4, minmax(0, 1fr));
        }
      }

      @media (min-width: 768px) {
        .blog-content {
          grid-template-columns: repeat(2, minmax(0, 1fr));
          column-gap: 1.5rem;
        }

        .blog-content > h2,
        .blog-content > h3,
        .blog-content > p,
        .blog-content > ul,
        .blog-content > ol,
        .blog-content > pre,
        .blog-content > table,
        .blog-content > .blog-table-wrap {
          grid-column: 1 / -1;
        }

        .blog-content > img {
          grid-column: 1 / -1;
          justify-self: center;
        }

        .blog-content > img:has(+ img),
        .blog-content > img + img {
          grid-column: span 1;
          justify-self: stretch;
          max-width: 100%;
        }
      }

      /* Legacy: multiple <img> inside one <p> */
      .blog-content > p:has(> img) {
        display: grid;
        grid-column: 1 / -1;
        grid-template-columns: 1fr;
        gap: 1.5rem;
        margin: 0 0 1rem 0;
      }
      @media (min-width: 768px) {
        .blog-content > p:has(> img + img) {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }
      .blog-content > p:has(> img) img {
        width: 100%;
        margin: 0;
        border-radius: 0.75rem;
        box-shadow:
          0 10px 15px -3px rgb(0 0 0 / 0.1),
          0 4px 6px -4px rgb(0 0 0 / 0.1);
        object-fit: cover;
      }
    </style>
  </head>
  <body>
    <div class="relative">
      <Navigation />
      <main>
        {/* Hero: article title with background image */}
        <section
          class="relative flex min-h-[45vh] flex-col justify-center bg-dark-bg bg-cover bg-center bg-no-repeat md:min-h-[30vh]"
          style={{
            backgroundImage: `url('${heroUrl}')`,
          }}
        >
          <div
            class="absolute inset-0 bg-gradient-to-r from-black/67 to-black/32 opacity-75"
          >
          </div>
        </section>

        {/* Article content */}
        <article class="bg-white">
          <div
            class="mx-auto max-w-6xl px-6 py-12 md:px-12 md:py-16 lg:px-16 lg:py-20 min-[1920px]:max-w-7xl"
          >
            <div class="w-full">
              {/* Meta: Zurück-Link, Titel, Autor, Datum */}
              <header class="border-b border-slate-300 pb-8">
                <Button
                  href={translatePath("/blog/")}
                  size="sm"
                  class="mb-4 gap-2"
                >
                  <span aria-hidden="true">←</span>
                  {t("blog.backToBlog")}
                </Button>
                <h1
                  class="font-anton text-2xl leading-tight text-dark md:text-3xl lg:text-4xl"
                >
                  {title}
                </h1>
                <div
                  class="mt-3 flex flex-wrap items-center gap-x-4 gap-y-1 font-outfit text-sm text-dark/60"
                >
                  <span>{author ?? t("blog.defaultAuthor")}</span>
                  <span class="text-dark/40" aria-hidden="true">·</span>
                  <time datetime={pubDate.toISOString()}>
                    {formatDate(pubDate, lang === "en" ? "en-GB" : "de-DE")}
                  </time>
                </div>
              </header>

              {/* Artikelinhalt */}
              <div
                class="blog-content pt-8 font-outfit text-base leading-relaxed text-dark/85 md:text-lg [&_a]:text-primary [&_a]:underline [&_a]:underline-offset-2 [&_a:hover]:text-primary-light [&_h2]:font-anton [&_h2]:mt-12 [&_h2]:mb-4 [&_h2]:text-2xl [&_h2]:md:text-3xl [&_h2]:text-dark [&_h3]:font-anton [&_h3]:mt-8 [&_h3]:text-xl [&_h3]:text-dark [&_p]:mb-4 [&_ul]:mb-4 [&_ul]:list-disc [&_ul]:pl-6 [&_ol]:mb-4 [&_ol]:list-decimal [&_ol]:pl-6 [&_pre]:mb-4 [&_pre]:overflow-x-auto [&_pre]:rounded-lg [&_pre]:bg-slate-100 [&_pre]:p-4 [&_code]:rounded [&_code]:bg-slate-100 [&_code]:px-1.5 [&_code]:py-0.5 [&_code]:font-mono [&_code]:text-sm [&_p_img]:my-4 [&_p_img]:rounded-xl [&_p_img]:shadow-lg [&_p_img]:object-cover"
              >
                <slot />
              </div>
            </div>
          </div>
        </article>
      </main>
      <Footer />
    </div>
  </body>
</html>
