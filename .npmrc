; @lucide/astro has not yet widened its peerDependencies range to Astro 7.
; The wrapper is API-stable and works with Astro 7, so allow installs to proceed
; despite the lagging peer range (applies locally and in the Cloudflare build).
legacy-peer-deps=true
